# シナリオ4 確率・場合の数テスト記録

- 実施日時: 2026-09-09
- 対象main（開始時）: `be523fffdb4e96531cfa68128df2eee811e8fd85`
- 対象: Guided Solution、固定類題、日次8問
- 評価区分: 固定合成入力による機能検証。独立AI模擬学習者の比較ではない。

## 追加した固定ケース

| ID | 操作・入力 | 期待 | 実際 | 証拠 | 判定 | 追加理由 |
| --- | --- | --- | --- | --- | --- | --- |
| S4-COUNT-001 | 最初の支援を表示 | 指定5項目をすべて表示 | 5項目を表示 | `test:probability-hints` | PASS | 共通の開始行動を固定するため |
| S4-COUNT-002 | 支援文の順序を検査 | 全体→順序→重複→条件→場合分け | 指定順で表示 | `test:probability-hints` | PASS | 個別計算前に数え方を設計させるため |
| S4-COUNT-003 | 最初の支援文を検査 | 数値計算・答えを含めない | 等号や個別の乗除式なし | `test:probability-hints` | PASS | ヒント1での答え漏洩を防ぐため |
| S4-COUNT-004 | 確率・場合の数の固定分野 | 両分野に適用 | `probability` と `counting` に適用 | `test:probability-hints` | PASS | 片方だけの適用漏れを防ぐため |
| S4-COUNT-005 | コイン、サイコロ、カード、移動列、スイッチ | 設定差があっても5項目を表示 | 全設定を対象判定 | `test:probability-hints` | PASS | 表面的設定による漏れを防ぐため |
| S4-COUNT-006 | 因数分解、円周角、売買 | 専用支援を表示しない | 対象外判定 | `test:probability-hints` | PASS | 過剰適用を防ぐ境界値 |
| S4-COUNT-007 | 固定類題・日次8問でヒント表示 | 同じ5項目を表示 | 共通関数を利用 | `test:probability-hints` | PASS | 画面による支援差を防ぐため |
| S4-COUNT-008 | Guided選択画面を開く | 個別の36通り・対称性より5項目を先に表示 | 構造確認を最初に表示 | `test:probability-hints` | PASS | 現行では個別解法が先に見えていたため |

## 指定記録様式

【シナリオ番号】

4：確率・場合の数

【学習者の事前状態】

固定合成入力として「場合分けがぐちゃぐちゃになります。」を想定。学習者役モデルは実行していない。

【使用した問題】

コイン、サイコロ、戻さないカード、三角形上の移動列、スイッチ選択を設定差のケースに使用。問題・正答集合は変更していない。

【学習者の入力】

上記の固定発言。画面上では最初の支援を開く操作に対応させた。

【GPTの主な対応】

変更前は「全体は36通り」「大小は対称」など個別問題の計算・解法へ先に進んだ。変更後は、全体、順序、重複、有利事象、場合分けの5点を確認してから計算へ進む。

【ヒント依存度】

機能が提示する支援は1（注目点）。固定合成入力のため、学習者の実依存度は未測定。

【良かった点】

個別問題の数値や答えを見せず、設定が変わっても再利用できる開始手順を明示する。既存の問題、正答、ID、採点、履歴・保存形式を変更しない。

【問題点】

5項目の表示は、学習者役が未見問題で自力に「全体は○通り」と開始できることの証拠ではない。

【Pre-test】

FAIL。最初の支援が個別問題の36通り・対称性・成立組へ進み、5点確認が共通手順になっていなかった。

【Post-test】

機能検証PASS。5項目を指定順で表示し、個別の数値計算・答えを含めない。

【Transfer test】

機能検証PASS。コイン、サイコロ、カード、移動列、スイッチへ適用し、非対象3分野を除外した。AI模擬学習者の転移評価は未実施。

【Delayed test】

未実施。

【同じミスの再発】

未測定。

【時間面の変化】

未測定。

【学習者が解法を言語化できたか】

未測定（○・△・×を付けない）。

【学習効果判定】

隔離AI模擬比較の残テスト。機能品質はPASS、学習効果の×・△・○・◎は未判定。

【改善すべきプロンプト・仕様】

同一の弱点profile・モデル条件で支援あり/なしを隔離し、未見Parallel Postと異設定Transferの最初の方針、全体数、順序・重複判断、場合分けの漏れを採点する。正解・評価基準を学習者役へ渡さない。

## 版差分

- 確率・場合の数専用の5項目確認ヒントを追加。
- Guided Solution、固定類題、日次8問で共通利用。
- 文章題との重複topicでは「場合の数」を優先し、移動列を文章題の立式支援へ誤分類しない。
- 問題文、正答、問題ID、採点、履歴・保存形式は変更なし。
