# 根号の準備問題：限定修正と再検証

確認日: 2026-09-09（英国時間）。基準main: f7010d2abd004ca275e244e51c562bffacd91735。

## シナリオ10 記録

【シナリオ番号】10（準備4の答案形式、部分）

【学習者の事前状態】実学習者ではなく、固定の合成入力。人間入力を待たず検証する。

【使用した問題】prep-4「√12 を a√b の形に簡単にしてください。」

【学習者の入力】√12 / sqrt(12) / √(4*3) / 2√3 / 2sqrt(3) / 2*√3 / 全角表記など。

【GPTの主な対応】対話GPTではなく準備問題の採点。基準ソースで√12がtrueと再現。値の一致を設問要求の達成と誤認していた。

【ヒント依存度】評価対象外（固定の機能テスト）。

【良かった点】既存の正解と入力表記を変えず、未簡約だけを準備4で通さない判定を追加。

【問題点】共通の数値同値判定だけでは形式要求を満たせない。準備3の6/8受理と、同じ誤答の再送で全解説を出す設計は変更しない。

【Pre-test】学習実験ではない。修正前の機能検査は√12を正解としてFAIL。

【Post-test】修正後の機能検査は24ケースPASS。学習前後比較とは呼ばない。

【Transfer test】AI模擬学習者の独立比較は未実施。他の準備問題の回帰確認は転移の証拠ではない。

【Delayed test】未実施。

【同じミスの再発】製品の同じ誤採点は新規テストで検出可能。学習者のミス再発率は今回評価しない。

【時間面の変化】評価対象外。

【学習者が解法を言語化できたか】今回評価しない。

【学習効果判定】機能品質PASS、AI模擬比較未実施。人間への効果は対象外。

【改善すべきプロンプト・仕様】評価者用基準と学習者入力を分離するv2方針を別紙に保存。合成入力テストをAI比較結果に読み替えない。

## 修正範囲・精査

- preflightのisAcceptedPrepAnswerをUIから呼ぶ。prep-4だけ簡約形の既存正解表記と照合する。
- √12 / sqrt(12) / √(4*3) / 小数近似 / 4√3/2 は通さない。2√3 / 2sqrt(3) / 2*√3 / 全角を受理。
- 問題ID、本文、正解、acceptedAnswers、保存キー、保存バージョン、履歴の処理は変更しない。共通採点関数も変更しない。
- 準備1/2/3/5は従来どおり。過去の完了状態を取り消さない。
- 新規24ケース、UI接続検査、問題配列非変更、保存ID維持を検証。production buildで必須化。
- Round 1: 新規検査、既存23コマンド、buildがPASS。追加修正0。
- Round 2: 差分・範囲限定の確認、移行、Level2、日次終了案内、学習キュー、先取り、日次固定の回帰はPASS。追加修正0。
- ブラウザの準備5問は前回完了済み。再入力のためのリセットは行わず、修正後の同じ操作のブラウザ再演は未実施とする。
- 本記録は公開候補の記録。公開成功・公開JS確認は実行結果として別途報告する。

## 総合

限定修正の機能評価はPASS。11シナリオ全体や学習効果の合格ではない。
優先Aは準備4誤採点の限定修正。優先Bは段階的ヒントと画面別答案形式。
「支援を受けた模擬学習者は追加支援なしの未見問題で、支援なし条件より適切な解答行動を示したか」:
今回は比較実験を行っていないため判定しない。人間の入力待ちではなく、自動比較テストの残タスクとして扱う。
