# v0.17.1 Learning Queue Consistency Audit

基準: v0.17.0（公開 v0.16.6 系統からの修正版）を再精査し、Home・弱点・PAST PAPER CYCLE・今日の課題・任意継続・翌日先取りの整合を一本化。

## 精査ループで見つけて修正した事項

### ループ1
- Home 内の `phaseAction()` が独自に次行動を決め、`nextLearningAction()` と二重化していた。
- 「元の誤答」に未回答も含まれていた。
- 弱点カードが最新年度基準、学習サイクルが別年度基準になる余地があった。

修正:
- Home の次行動判断を共通ルートへ統一。
- 表示を「未解決問題」に統一。
- 誤答・未回答の内訳を表示。
- 最初に残っている未解決年度を弱点カードの正本にした。

### ループ2
- 今日の課題と翌日先取りが `buildTodayTaskCandidates()`、次のアクションが別ルートで、先頭課題が一致しない場合があった。

修正:
- `buildLearningQueue()` を追加。
- 途中の採点・過去問を最優先。
- 未解決問題は該当年度の具体的な1問を先頭にする。
- 類題補強は該当年度のpractice課題を優先。
- 今日の必須、任意の次1件、翌日先取りが同じキューを参照。

### ループ3
- `nextLearningAction()` 自体が過去問ドラフトを認識しないため、Home と採点結果画面で次行動がずれる余地があった。

修正:
- `resumeDraftAction()` を `learningRoute.ts` に共通化。
- `nextLearningAction()` が途中採点・途中過去問を最優先するよう統一。

### ループ4
- 更新前に固定済みだった当日計画・翌日先取り計画は、旧順序のまま残るため、更新直後だけ不整合が継続する可能性があった。

修正:
- 日次計画・先取り計画に `queueVersion: 2` を追加。
- 旧計画は完了済み件数を維持したまま、共通キュー順へ一度だけ再整列。
- 学習履歴そのものは変更しない。

### ループ5
- v0.17.1への版上げ後、self-check が v0.17.0 の固定値を期待して失敗。

修正:
- version / public version / package-lock / self-check を v0.17.1 に揃えた。

## 最終仕様
- A=60点 / B=70点 / C=75点を維持。
- 1日最大10課題を維持。
- 今日の10件を途中で11件目へ自動補充しない。
- 必須終了後は同じ学習キューの次1件へ任意で進める。
- 翌日先取りも同じキューの続きから最大10件。
- 「いま直す弱点」は未解決総数と誤答/未回答内訳を表示し、優先3問を提示。
- PAST PAPER CYCLE の現在推奨も同じキューの先頭を表示。
- 旧日次・先取り計画は更新直後に共通キューへ再整列。
- dataVersion 5を維持し、既存学習履歴は非破壊。

## 2回連続精査ルール
最終修正後、以下を2回連続で実行し、両方とも修正事項0件。

- syntax-check
- self-check
- today-plan
- daily-cap-continue
- study-ahead
- v0.17 regression
- learning-queue-consistency
- migration preservation
- mastery reactivation
- goal ETA
- goal ETA runtime
- remediation difficulty audit

### 最終精査1
修正事項: 0件

### 最終精査2
修正事項: 0件

## Build注記
この環境にはプロジェクトの `node_modules` がないため、完全な `npm run build` は実行していない。
公開前は通常環境で `npm ci` → `npm run build` を実行すること。
