# v0.17.10 実ブラウザ報告不具合 修正実装レポート

## 起点確認

- 公開URL: https://fyam8.github.io/waseshibu-math/
- GitHub: https://github.com/FYam8/waseshibu-math
- ユーザー指定のテスト起点: v0.17.9
- 修正元ソース: この作業で直前に2周精査済みだった v0.17.9 ZIP

### GitHubとの不一致
2026-09-02時点でWeb取得したGitHub `main` は、
- `package.json`: 0.17.7
- `src/version.ts`: 0.14.0
を返しており、ユーザー指定の「GitHub現行v0.17.9」と一致しなかった。

そのため、古いGitHub内容へ巻き戻さず、このチャット内で確定していたv0.17.9ソースを修正起点とした。
公開前には、GitHub main / Pages / version.json のv0.17.10同期確認が必要。

## バージョン
- appVersion: 0.17.10
- dataVersion: 6（変更なし）
- storage schema migration: 追加なし

既存localStorageキー・学習データ形式は変更していない。

## BUG-MATH-01 類題2/4 Home再開導線
- `inProgressRemediations()` をHomeで読み込み。
- 2022〜2026の進行中類題を専用カードで表示。
- `元問題ID + source + topic` を含むURLへ直接再開。
- 主ルートの未解決問題優先は維持しつつ、進行中2/4を埋没させない。

## BUG-MATH-02 Guided一時状態の別問題漏れ
`App.tsx` で `/guided-review` を `pathname + search` をkeyにして再マウント。
問題ID変更・URL変更・戻る/進むで前問のReact一時stateを再利用しない。

## BUG-MATH-03 補強/類題一時状態の別分野漏れ
`/remediate` と `/reinforce` も `pathname + search` をkeyにして再マウント。
入力、submitted、正誤、解説、CTA等の前ルート一時stateを別URLへ持ち越さない。

## BUG-MATH-04 「今日の数学は完了」表示
進行中類題がある場合、
- 「今日の予定分は完了しました」
- 「学習全体には続きがあります」
- 類題進捗が保存済みであること
を明示する文言に変更。

## BUG-MATH-05 克服済みと得点回収候補
`Report.tsx` でGuided masteryを確認し、
- 未解決・学習中の得点回収候補
- 克服済み・定着確認候補
を分離表示。

## BUG-MATH-06 2019〜2021任意年度スコア
履歴表示は2019〜2021を `任意演習 参考スコア` として優先表示。
既存のscoreValidity・露出データ自体は変更しない。

## BUG-MATH-07 相似比の条件不足
修正前:
「相似比が3:2です。小さい図形の周...」

修正後:
「大きい図形と小さい図形の相似比が3:2です。小さい図形の周...」

比の順序を明記し、正答30が一意になるようにした。

## BUG-MATH-08 2024-Q1-5 図形類題
4問を元問題の中心技能に寄せて再設計。

1. 正方形＋正三角形の角の差
2. 一直線180°＋複数既知角
3. 正方形の対角線45°＋正三角形60°
4. 正方形＋正三角形の外側角（150°）

`RemediationFigure.tsx` を追加し、2024-Q1-5だけに問題ごとのSVG模式図を表示。
SVGは外部画像生成を使わず、点名・既知角・求める角をコードで固定。
「模式図・縮尺どおりとは限らない」注意書きを表示。

## データ保護
今回の修正では新しい永続化キー・保存形式を追加していない。
dataVersionは6のまま。
既存の以下は削除・初期化しない。
- attempts
- exam scores
- drafts
- exposure
- Guided history/progress
- remediation progress
- mastery
- daily progress
- A/B/C preferences

## 追加テスト
`npm run test:v01710-browser-bugs`

対象:
- Guided route key
- Remediation route key
- Reinforcement route key
- Home 2/4再開導線
- 今日完了文言
- 得点回収候補の状態分離
- 任意年度参考スコア表示
- 相似条件一意性
- 2024-Q1-5中心技能
- 図形SVG紐付け
- v0.17.10整合

## 自動テスト結果
- `npm run syntax-check`: PASS（49 TS/TSX）
- `npm run test:v01710-browser-bugs`: PASS
- `npm run test:v0178:all`: PASS × 2周連続
- 23 non-build commands: PASS
- 160/160 core-skill: PASS
- 640 remediation content: PASS
- migration / mastery / today / ETA / year-role / user-scenarios / Guided / remediation progress: PASS

## build
`npm run build`: FAIL / BLOCKED

このコンテナではReact / react-router-dom / JSX型定義が欠けており、
従来と同様 `JSX.IntrinsicElements` 等で停止する。
正常な依存環境で `npm ci && npm run build` の確認が必要。

## 実ブラウザ未検証
この環境には対話型ブラウザ操作機能がないため、以下はPASS扱いしていない。
- Export→fresh profile Importの完全復元
- 390×844 / 375×667 / 430×932
- 実ブラウザ第1周CLEAN
- 実ブラウザ第2周CLEAN
- pageerror/console errorの実測
- GitHub Pages配信後のv0.17.10確認

## 公開判定
NO（現時点）

理由:
- build未確認
- Export/Import E2E未確認
- smartphone E2E未確認
- 実ブラウザ2回連続CLEAN未実施
- GitHub mainと指定v0.17.9の不一致を解消していない
