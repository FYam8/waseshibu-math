# v0.17.6 ユーザー視点 精査ループ監査

## 今回ユーザー視点で追加発見・修正した事項

1. **旧年度補強で不正解でも「完了」扱いになる**
   - `Reinforcement.tsx` の補強問題採点で、正誤に関係なく `markOldQuestionCompleted()` が呼ばれていた。
   - 不正解では完了にせず、正解時のみ完了へ進むよう修正。

2. **正答表示中に、その答えを写して自動採点できる**
   - 正答を表示している間は自動採点を無効化。
   - 正答を閉じる際に入力と直前採点結果をリセットし、もう一度自力で入力して正解した場合だけ完了にする。
   - UI文言も「正答を見ただけでは完了にならない」挙動と一致させた。

3. **バージョン整合 self-check の更新漏れ**
   - v0.17.6への更新後も self-check が0.17.5固定だったため修正。

## 最終2回連続精査

最終修正・監査文書追加後、以下の同一テスト一式を変更なしで2回連続実行。

- syntax-check
- self-check
- test:migration
- test:mastery
- test:today
- test:eta
- test:eta:runtime
- audit:remediation
- test:v017
- test:year-role
- test:user-scenarios
- test:user-perspective
- test:user-perspective:v0176

### 最終精査1
全13項目 PASS / 修正事項0件

### 最終精査2
全13項目 PASS / 修正事項0件

## 補足
`verify-critical` / 完全なVite buildは、実行環境にReact等のnode_modulesがないため環境依存で実行完了できない。
公開前に依存関係のある通常環境で `npm ci` → `npm run build` と `npm run verify-critical` を実行すること。
