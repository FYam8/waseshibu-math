# v0.17.4 ユーザー視点精査監査

## 今回の精査範囲
生徒が実際に使う流れを基準に、年度を開く、途中で離脱する、任意旧年度を解く、主学習ドラフトへ戻る、改善確認年度を選ぶ、旧年度補強へ進む、採点する、という状態遷移を確認した。

## 精査ループ中に発見して修正した事項
1. 年度通し画面を開いた履歴 `exposure-YYYY` が `yearExposureState()` に反映されず、見た年度が `untouched` のままになる問題を修正。
2. 2019〜2023年度を通しで開いて全問題を見ても、旧年度補強候補では各小問が未使用扱いのままになる問題を修正。年度通し表示では全小問を露出済みとして扱う。
3. 上記露出修正だけを行うと、初見の年度をその場で初めて開いて解き切った同一セッションまで「参考スコア」になってしまう問題を追加発見し修正。
   - 年度を開く前の `firstLookEligible` をドラフトに保存。
   - 同じ初見受験を途中保存・再開しても初見資格を維持。
   - 既に別経路で露出済み、または過去に完了済みなら参考スコア。
4. `readDraft()` がドラフト未存在時も `{answers:{}}` を返し、露出済みでも初見資格推定を誤る可能性を修正。未存在時は `{}` を返し、旧ドラフト形式だけ互換変換する。

## ユーザー視点シナリオ
- 2025年度を開いて見た後は未使用年度扱いに戻らない。
- 2025が露出済み、2026が未露出なら改善確認は2026を優先。
- 2021年度を通しで開いた後、その年度の小問を「未使用の旧年度補強」として再提示しない。
- 2019任意演習の誤答は2024以降の必須学習キューを横取りしない。
- 2019任意ドラフトと2024主学習ドラフトが共存しても主学習の続きが優先され、任意ドラフトは保持される。
- 初見年度を初めて開いてその受験を継続している間は、画面表示による露出イベントと「同一初見受験」を区別し、初見スコア資格を維持する。
- 既露出年度を新たに通し受験した場合は参考スコアにする。

## データ互換
dataVersion は 5 のまま。既存学習履歴を破壊しない。

## 最終確認
最終修正・監査文書・version更新後に、全回帰テスト + ユーザー視点シナリオを2回連続で実行する。
修正が出た場合は連続0件カウントをリセットする。
